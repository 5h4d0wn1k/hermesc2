"""Hermes C2 - lab-scoped C2 & post-exploitation education framework.

This package is an AUTHORIZED TESTING / EDUCATION tool. It is hard-gated to the
loopback interface (127.0.0.1) and `lab-*` allowlisted names. It never connects
to any third-party host, never persists on any machine, and never executes
anything outside its own bundled sandboxed agent fixture (sample_agent.py).

Read README.md before use.
"""

__version__ = "1.0.0"
__title__ = "hermesc2"
PROGRAM_NAME = "hermesc2"